extends trip

block trip_content
    h2 Destinations
    p kys
    //- each user in guests
    //-     li 
    //-      | #{user.name} 
    //-      if user.isMe
    //-         | (you) 
    //-      if user.isOwner
    //-         | (owner)
    